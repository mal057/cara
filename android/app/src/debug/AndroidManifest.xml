<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET permission intentionally omitted.
         Sola is a privacy-first, fully offline app. No network access is permitted
         in any build variant. Flutter hot reload / DevTools connectivity is
         handled via ADB local port forwarding and does NOT require the
         INTERNET manifest permission on the device. -->
</manifest>
